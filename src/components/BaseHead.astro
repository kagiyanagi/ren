---
import { ClientRouter } from "astro:transitions";
import "@/styles/global.css";
import {
  SITE_DESCRIPTION,
  KNOWN_TECH,
  NAV_LINKS,
  SEO_KEYWORDS,
  SITE_TITLE,
  TWITTER_HANDLE,
} from "@/consts";

interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const {
  title = SITE_TITLE,
  description = SITE_DESCRIPTION,
  image = "/image.jpg",
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);

// Read at build time from Vercel env vars (or local .env). Empty means skip.
const googleSiteVerification = (
  import.meta.env.GOOGLE_SITE_VERIFICATION ??
  process.env.GOOGLE_SITE_VERIFICATION ??
  ""
).trim();

const socialLinks = NAV_LINKS.map((link) => link.href)
  .filter((href): href is string => Boolean(href))
  .map((href) => (href.startsWith("http") ? href : `https:${href}`));

const structuredData = [
  {
    "@context": "https://schema.org",
    "@type": "Person",
    name: SITE_TITLE,
    alternateName: ["Aqua", "Kagiyanagi", "Ren"],
    description: SITE_DESCRIPTION,
    url: canonicalURL.href,
    sameAs: socialLinks,
    knowsAbout: KNOWN_TECH,
  },
  {
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: SITE_TITLE,
    url: Astro.site?.href,
    description,
    publisher: {
      "@type": "Person",
      name: SITE_TITLE,
    },
  },
];
---

<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<link rel="icon" href="/favicon.ico" type="image/x-icon" />
<link rel="icon" href="/muichiro.svg" type="image/svg+xml" />
<link rel="shortcut icon" href="/favicon.ico" />
<meta name="generator" content={Astro.generator} />
<link rel="sitemap" href="/sitemap-index.xml" />

<link
  rel="preload"
  href="/fonts/VCRosdNEUE.ttf"
  as="font"
  type="font/ttf"
  crossorigin
/>

{
  googleSiteVerification && (
    <meta name="google-site-verification" content={googleSiteVerification} />
  )
}

<link rel="canonical" href={canonicalURL} />

<title>{title}</title>
<meta name="title" content={title} />
<meta name="description" content={description} />
<meta name="keywords" content={SEO_KEYWORDS} />
<meta name="author" content={SITE_TITLE} />
<meta
  name="robots"
  content="index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1"
/>
<meta name="application-name" content={SITE_TITLE} />
<meta name="theme-color" content="#000000" />

<meta property="og:type" content="website" />
<meta property="og:url" content={Astro.url} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={new URL(image, Astro.url)} />
<meta property="og:site_name" content={SITE_TITLE} />
<meta property="og:locale" content="en_US" />

<meta property="twitter:card" content="summary_large_image" />
<meta property="twitter:url" content={Astro.url} />
<meta property="twitter:title" content={title} />
<meta property="twitter:description" content={description} />
<meta property="twitter:image" content={new URL(image, Astro.url)} />
<meta property="twitter:site" content={TWITTER_HANDLE} />

<script type="application/ld+json" set:html={JSON.stringify(structuredData)} />

<ClientRouter />
